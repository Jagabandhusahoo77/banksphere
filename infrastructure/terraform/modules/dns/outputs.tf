# (No outputs needed — this module is a pure leaf that only writes
# records; nothing downstream consumes anything from it. Kept as an
# explicit empty file rather than deleted, so its presence/absence isn't
# itself a signal of anything.)
