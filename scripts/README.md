# Scripts

Helper scripts for local development and CI/CD, grouped by concern.

```text
scripts/
├── local/       Local dev environment setup/teardown
├── docker/      Docker build/run helpers
├── kubernetes/  kubectl/Helm convenience scripts
├── terraform/   Terraform plan/apply wrappers
└── ci/          Scripts invoked from CI pipelines
```

No scripts have been implemented yet.
